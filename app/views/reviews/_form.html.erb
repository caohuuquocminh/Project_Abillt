<%= simple_form_for([@app, @app.reviews.build]) do |f| %>
	<div id="rating-form">
		<label>Wertung</label>
	</div>
	<%= f.input :comment %>
	<%= f.button :submit %>

<% end %>
<script>
	$('#rating-form').raty({
		path: '/assets/',
		scoreName: 'review[rating]'
	});
</script>

