<%= simple_form_for @app, :html => { :multipart => true } do |f| %>
	<%= select_tag(:category_id, options_for_select(@categories), :prompt => "Wählen Sie eine Kategorie") %>
	<%= f.file_field :app_img %>
	<%= f.input :name, label: "Name der anwendung" %>
	<%= f.input :description %>
	<%= f.input :link %>
	<%= f.input :developer %>
	<%= f.input :operation %>
	<%= f.input :price %>
	<%= f.button :submit %>

<% end %>

